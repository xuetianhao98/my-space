---
import { getPublishedPosts } from "../../lib/blog";

const [latestPost] = await getPublishedPosts();

if (!latestPost) {
  return Astro.redirect("/");
}

return Astro.redirect(`/blog/${latestPost.id}/`);
---
